import type { AppContent } from '../types/tab';
import terminal from '@/assets/images/terminal.png';

export const apps: AppContent[] = [
  {
    id: 'terminal',
    icon: terminal,
    name: 'Le gentil terminal de Luis',
  },
];
